spring:
  application:
    name: user-service

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://pg_db:5432/user_service?currentSchema=security
    username: user_app_user
    password: 123

server:
  port: 8080
